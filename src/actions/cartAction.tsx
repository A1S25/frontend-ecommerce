import {
  ADD_TO_CART,REMOVE_CART_ITEM,SAVE_SHIPPING_INFO,
} from "../constants/cartConstants";
import axios from "axios";

// Add to Cart
export const addItemsToCart = (id:any, quantity:any) => async (dispatch: any) => {
 
    dispatch({  });
  
};

// REMOVE FROM CART
export const removeItemsFromCart = (id:any) => async (dispatch: any) => {
  try{
    dispatch({  });
  }catch(error){

  }
};

// SAVE SHIPPING INFO
export const saveShippingInfo = (data:any) => async (dispatch: any) => {
  try{
    dispatch({  });
  }catch(error){

  }
};

export{}